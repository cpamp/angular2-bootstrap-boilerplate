{
  "name": "angular-2-bootstrap-template",
  "version": "1.0.0",
  "description": "An angular 2 template using bootstrap",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start-win",
    "start-win": "tsc && http-server -o",
    "start-unix": "tsc | http-server -o",
    "postinstall": "typings install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "angular2": "^2.0.0-beta.17",
    "bootstrap": "^3.3.6"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
